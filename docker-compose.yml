version: '3.8'

services:
  app:
    build:
      context: ./backend
    container_name: laravel_app
    working_dir: /var/www
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/var/www
    command: php artisan serve --host=0.0.0.0 --port=8000

volumes:
  db_data:
